### CustomHooks and HTTP Reqs

CustomHooks can be used to reuse logics and refactor the code.

HTTPs reqs are also being done.

The Firebase API is used to contact with the created Firebase Realtime Database
